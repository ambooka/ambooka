import { Database, FileCode2, RefreshCcw, ShieldCheck } from 'lucide-react'
const commands=["export DATABASE_URL='postgresql://USER:PASSWORD@HOST:PORT/postgres'",'npm run db:reset','npm run dev']
export default function AdminSchemaPage(){return <div className="space-y-8"><section className="rounded-[2rem] border border-slate-800 bg-slate-950 p-6 md:p-8"><p className="text-xs font-semibold uppercase tracking-[0.3em] text-sky-300">Fresh database rebuild</p><h1 className="mt-3 text-3xl font-black tracking-[-0.04em] text-white md:text-5xl">Drop, recreate, seed</h1><p className="mt-3 max-w-3xl text-sm leading-7 text-slate-400">The project includes one canonical reset script: <code className="text-sky-300">supabase/reset-and-seed.sql</code>. It drops the public schema and recreates the portfolio database cleanly with resume-aligned tables, policies, indexes and seed content.</p></section><section className="grid gap-4 lg:grid-cols-3"><Card icon={<Database size={20}/>} title="Clean schema" body="Recreates personal info, skills, experience, education, projects, case studies, metrics, blog posts, certifications and contact messages."/><Card icon={<ShieldCheck size={20}/>} title="Safe policies" body="Public reads are allowed for portfolio content, contact insert is allowed, authenticated users can manage content."/><Card icon={<RefreshCcw size={20}/>} title="Repeatable reset" body="Useful while the portfolio is being rebuilt. Run it whenever the schema becomes messy during development."/></section><section className="rounded-[2rem] border border-slate-800 bg-slate-950 p-6"><h2 className="text-xl font-bold text-white">Run order</h2><div className="mt-4 space-y-3">{commands.map(c=><pre key={c} className="overflow-x-auto rounded-2xl border border-slate-800 bg-[#070A0F] p-4 text-sm text-sky-200"><code>{c}</code></pre>)}</div></section><section className="rounded-[2rem] border border-slate-800 bg-slate-950 p-6"><div className="flex items-start gap-4"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-sky-400 text-slate-950"><FileCode2 size={20}/></div><div><h2 className="text-xl font-bold text-white">Files added</h2><ul className="mt-3 space-y-2 text-sm text-slate-400"><li><code className="text-sky-300">supabase/reset-and-seed.sql</code> — destructive full reset + seed.</li><li><code className="text-sky-300">docs/database-reset.md</code> — reset instructions.</li><li><code className="text-sky-300">scripts/verify-no-playful-overlay.mjs</code> — prevents playful inspection effects from returning.</li></ul></div></div></section></div>}
function Card({icon,title,body}:{icon:React.ReactNode;title:string;body:string}){return <article className="rounded-3xl border border-slate-800 bg-slate-950 p-6"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-sky-400 text-slate-950">{icon}</div><h2 className="mt-4 text-lg font-bold text-white">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{body}</p></article>}
